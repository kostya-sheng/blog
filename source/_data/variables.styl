// Custom variables, loaded after NexT's (custom_file_path.variable in _config.next.yml).

// Pisces widens the page to 73% of the window on large screens, which puts
// ~60 Chinese characters on a line at 1920px and ~85 at 2560px.
// A fixed width keeps the post text at ~40 characters per line.
$content-desktop-large   = 1080px;
$content-desktop-largest = 1080px;
